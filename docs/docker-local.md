###  Docker — Local Commands
#### Reference commands used during local Docker testing.
* Build Docker Image Locally

```
docker build -t ayodeji-app .
```
```
docker run -p 5000:5000 ayodeji-app
```
Access locally:
``` 
http://localhost:5000
```